export const metadata = {
  title: 'Directory',
  description: 'Search and filter every guide, tool, and resource across the key hubs.',
  robots: { index: false, follow: true },
}

export default function DirectoryLayout({ children }) {
  return children
}
